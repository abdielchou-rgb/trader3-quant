# Core runtime (required)
numpy>=1.24
pandas>=2.0
scipy>=1.10
pyyaml>=6.0

# Quant stack (needed by signal/regime/ensemble paths)
hmmlearn>=0.3
scikit-learn>=1.3

# HTTP client for broker adapters (tiger_broker)
aiohttp>=3.9

# Deep scorer backend (evolve/core/deep_model.py; missing → flagged numpy fallback)
torch>=2.0

# Test toolchain
pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=4.0
ruff>=0.4
mypy>=1.8

# Optional extras (uncomment as needed):
# fastapi>=0.100 ; uvicorn>=0.20    # API server (trader3.api)
# cvxpy>=1.4                        # optimizer acceleration
# pyarrow>=14                       # minute-data parquet backend
# qlib>=1.0                         # optional backtest engine
# riskfolio-lib>=6                  # HRP optimizer
# lightgbm / xgboost                 # MoE ensemble experts
# akshare / baostock                # alt data sources
